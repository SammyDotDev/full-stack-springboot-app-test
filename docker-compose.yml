services:
  db:
    container_name: re-postgres-spring-boot
    image: postgres:latest
    environment:
      POSTGRES_USER: nazacodes
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    volumes:
      - db:/data/postgres
    ports:
      - "5232:5432"
    networks:
      - db
    restart: unless-stopped

networks:
  db:
    driver: bridge

volumes:
  db: